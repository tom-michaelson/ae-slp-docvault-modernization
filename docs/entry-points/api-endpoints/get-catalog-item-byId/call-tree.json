{
  "entryPoint": {
    "method": "Microsoft.eShopWeb.PublicApi.CatalogItemEndpoints.CatalogItemGetByIdEndpoint.HandleAsync",
    "file": "src/PublicApi/CatalogItemEndpoints/CatalogItemGetByIdEndpoint.cs",
    "line": 34,
    "signature": "Task<IResult> HandleAsync(GetByIdCatalogItemRequest request, IRepository<CatalogItem> itemRepository)"
  },
  "calls": [
    {
      "method": "IRepository<CatalogItem>.GetByIdAsync(request.CatalogItemId)",
      "db": {"table": "CatalogItems", "op": "select"},
      "notes": "SELECT by primary key. Returns null when no row exists — causes immediate HTTP 404 return before further processing."
    },
    {
      "method": "IUriComposer.ComposePicUri(item.PictureUri)",
      "notes": "Leaf — rewrites the stored relative PictureUri to an absolute URL. Called only when item is found (non-null path)."
    }
  ]
}

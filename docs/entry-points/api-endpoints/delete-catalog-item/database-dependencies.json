[
  {
    "type": "table",
    "name": "CatalogItems",
    "key": "catalog-items",
    "operations": ["select", "delete"],
    "columns": ["Id"],
    "locations": [
      "src/ApplicationCore/Entities/CatalogItem.cs",
      "src/Infrastructure/Data/Migrations/20201202111507_InitialModel.cs"
    ],
    "notes": [
      "Fetched by primary key via GetByIdAsync to confirm existence; returns 404 immediately if the row does not exist.",
      "Deleted by primary key via DeleteAsync. BasketItems.CatalogItemId references this table — the database referential action (RESTRICT or CASCADE) determines whether a delete fails if active basket items reference the item."
    ]
  }
]

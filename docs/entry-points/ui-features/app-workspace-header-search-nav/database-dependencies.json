[
  {
    "type": "table",
    "name": "documents_v2",
    "key": "documents-v2",
    "operations": ["select"],
    "columns": ["id", "name", "file_type", "file_path", "tags", "uploaded_at", "uploaded_by"],
    "locations": [
      "backend/src/db/migrations/002-create-documents-v2.sql",
      "backend/src/services/FallbackSearchProvider.js",
      "backend/src/routes/search.js"
    ],
    "notes": [
      "Queried via ILIKE pattern match on the name column only: WHERE name ILIKE '%{query}%' ORDER BY uploaded_at DESC. No full-text index is used; IndexManager always falls through to this table.",
      "All columns are returned via SELECT *; the service layer adds a synthetic score field (always 1.0) before returning results to the frontend."
    ]
  }
]

[
  {
    "type": "table",
    "name": "documents_v2",
    "key": "documents-v2",
    "operations": ["update"],
    "columns": ["id", "tags"],
    "locations": [
      "backend/src/routes/tags.js",
      "backend/src/db/migrations/002-create-documents-v2.sql"
    ],
    "notes": [
      "The PUT handler issues UPDATE documents_v2 SET tags = $1 WHERE id = $2 RETURNING id, tags; the tags column is a PostgreSQL TEXT[] array.",
      "Row is looked up solely by the document UUID passed as a URL path parameter; the operation returns 404 if no matching row exists."
    ]
  }
]

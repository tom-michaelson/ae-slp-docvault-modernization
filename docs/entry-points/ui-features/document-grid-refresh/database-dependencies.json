[
  {
    "type": "table",
    "name": "documents_v2",
    "key": "documents-v2",
    "operations": ["select"],
    "columns": ["id", "name", "file_type", "file_path", "tags", "uploaded_at", "uploaded_by"],
    "locations": [
      "backend/src/db/migrations/002-create-documents-v2.sql",
      "backend/src/routes/documents.js"
    ],
    "notes": [
      "All columns fetched via SELECT * ORDER BY uploaded_at DESC; the full row set replaces the client-side documents array on every refresh.",
      "tags column is TEXT[] and may be null for rows inserted via the documents→documents_v2 sync trigger (003-create-trigger.sql deliberately omits tags from the trigger INSERT)."
    ]
  }
]
